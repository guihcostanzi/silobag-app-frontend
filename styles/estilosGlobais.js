import { StyleSheet } from "react-native";

export const estilosApp = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: "#000",
  },
  tituloPagina: {
    fontSize: 24,
    fontWeight: 'bold',
    color: '#fff',
    textAlign: 'center',
    marginVertical: 15,
  },
  subtitulo: {
    fontSize: 18,
    color: '#ccc',
    textAlign: 'center',
    marginBottom: 10,
  }
});